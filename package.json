{
  "name": "fintrack-qa-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node app/server.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
